eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  security:
    enabled: false

logging:
  level:
    root: off

evcache:
  sidecar:
    group: test
    hostname: localhost
    port: 11211
