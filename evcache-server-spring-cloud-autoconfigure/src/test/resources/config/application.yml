eureka:
  instance:
    prefer-ip-address: true
    data-center-info:

  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    root: off

evcache:
  asg-name: test
