eureka:
  instance:
    prefer-ip-address: true
    data-center-info:

  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  security:
    enabled: false

logging:
  level:
    root: off

evcache:
  asg-name: test
