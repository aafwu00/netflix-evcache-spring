dependencies {
    implementation project(':evcache-client-spring')

    implementation "com.netflix.evcache:evcache-core:${project.ext['evcache.version']}"

    implementation 'org.springframework.boot:spring-boot-starter-web'
}
